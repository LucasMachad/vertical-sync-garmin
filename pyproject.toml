[project]
name = "vertical-sync"
version = "0.1.0"
description = "Fetch and analyze Garmin training data to prepare a trail running plan"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Corentin Giraud" }]
requires-python = ">=3.12"
dependencies = [
    "click>=8.1",
    "fitdecode>=0.11.0",
    "garminconnect>=0.3.6",
    "pandas>=3.0.2",
    "python-dotenv>=1.2.2",
]

[project.scripts]
vs = "vertical_sync.cli:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vertical_sync"]

[dependency-groups]
dev = [
    "weasyprint>=68.1",
]
